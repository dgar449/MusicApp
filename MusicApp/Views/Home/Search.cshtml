@using MusicApp.Controllers;
@using MusicApp.Data.ViewModels;
@using System.Web;
@model IEnumerable<SearchSongVm>;

@{
    ViewBag.Title = "Search"; 
}

    @*<form asp-controller="Home" asp-action="Search" method="get">
    
    <label>Search</label>
    <input  type="text" name="sq" />

    <div>
        <button type="submit" asp-action="Search" > Search </button>
    </div>
    
</form>*@
    <div>
        @foreach (var SearchSongVm in Model)
        {           
                <div >
                    <h3>@SearchSongVm.SongTitle</h3>
                </div>
                <div>
                    <h3>@SearchSongVm.ArtistName</h3>
                    </div>
                     <div>
                    <h3>@SearchSongVm.AlbumName</h3>
                    </div>
                     <div>
                    <h3>@SearchSongVm.TrackLength</h3>
                    </div>
                <img class="album-art" src="~/images/coldplay.png" max-width="50px" height="auto" alt="aa" />
               
        }
        </div>